extensions:
    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  blacklist:
      - config
  format:
    - html
    - clover
  output:
    html: build/coverage
    clover: build/logs/clover.xml